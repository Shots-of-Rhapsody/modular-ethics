# =============================================================================
# Ethics Testbed — .gitattributes
# Normalize line endings and handle binary/text correctly
# =============================================================================

# Default: auto-detect text files and normalize to LF in repo
* text=auto

# ---------------------------------------------------------------------------
# Line endings
# ---------------------------------------------------------------------------
# Always use LF for source code, HTML, CSS, JSON, Markdown
*.js   text eol=lf
*.mjs  text eol=lf
*.json text eol=lf
*.css  text eol=lf
*.html text eol=lf
*.md   text eol=lf
*.yml  text eol=lf
*.yaml text eol=lf

# Dart & Flutter project
*.dart text eol=lf
*.kt   text eol=lf
*.kts  text eol=lf
*.gradle text eol=lf
*.properties text eol=lf
*.xml  text eol=lf
*.cc   text eol=lf
*.cpp  text eol=lf
*.c    text eol=lf
*.h    text eol=lf

# Scripts
*.sh   text eol=lf
*.ps1  text eol=crlf

# ---------------------------------------------------------------------------
# Binary assets
# ---------------------------------------------------------------------------
*.png  binary
*.jpg  binary
*.jpeg binary
*.ico  binary
*.exe  binary
*.dll  binary

# ---------------------------------------------------------------------------
# Git LFS (if you want to track large files in LFS)
# These lines keep your old rules and extend them.
# ---------------------------------------------------------------------------
*.ads filter=lfs diff=lfs merge=lfs -text
*.adp filter=lfs diff=lfs merge=lfs -text

# You can add other heavy assets if needed, e.g.:
# *.mp4 filter=lfs diff=lfs merge=lfs -text
# *.zip filter=lfs diff=lfs merge=lfs -text
